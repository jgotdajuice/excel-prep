---
phase: 08-challenge-components-prompt-formatting
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/components/RightPanel.tsx
  - src/pages/ChallengePage.tsx
  - src/components/DrillQuestion.tsx
  - src/pages/DrillPage.tsx
  - src/index.css
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - "Challenge prompts render with structured layout: scenario text, styled data block, task instruction"
    - "Drill question prompts use the same structured formatting as challenge prompts"
    - "RightPanel uses Tailwind utilities with no CSS class references from index.css"
    - "ChallengePage layout uses Tailwind utilities for the three-column layout"
    - "DrillQuestionCard uses Tailwind light-theme utilities instead of dark inline styles"
    - "Handsontable grid still renders correctly with proper height and borders"
    - "Old challenge CSS class definitions are removed from index.css"
  artifacts:
    - path: "src/components/RightPanel.tsx"
      provides: "Tailwind-migrated right panel with structured prompt display"
    - path: "src/pages/ChallengePage.tsx"
      provides: "Tailwind-migrated challenge page layout"
    - path: "src/components/DrillQuestion.tsx"
      provides: "Tailwind-migrated drill question card (light theme)"
    - path: "src/index.css"
      provides: "Cleaned CSS with challenge class definitions removed"
  key_links:
    - from: "src/components/RightPanel.tsx"
      to: "src/utils/formatPrompt.ts"
      via: "import and render PromptDisplay"
      pattern: "PromptDisplay|formatPrompt"
    - from: "src/pages/DrillPage.tsx"
      to: "src/utils/formatPrompt.ts"
      via: "import and render PromptDisplay for drill prompts"
      pattern: "PromptDisplay|formatPrompt"
    - from: "src/pages/ChallengePage.tsx"
      to: "src/components/SpreadsheetGrid"
      via: "flex layout with explicit height propagation"
      pattern: "flex.*h-full|challenge-grid-area|flex-1.*overflow-hidden"
---

<objective>
Migrate RightPanel and ChallengePage layout to Tailwind, integrate structured prompt formatting for both challenge and drill prompts, restyle DrillQuestionCard to light theme, and remove old CSS class definitions from index.css.

Purpose: Complete the Phase 8 migration — all challenge components use Tailwind, prompts render as structured sections instead of wall-of-text, and dead CSS is cleaned up. This is the highest-risk plan due to Handsontable adjacency.
Output: Fully migrated challenge components, structured prompt rendering, clean index.css
</objective>

<execution_context>
@/Users/jam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-challenge-components-prompt-formatting/08-01-SUMMARY.md

@src/index.css
@src/components/RightPanel.tsx
@src/pages/ChallengePage.tsx
@src/components/DrillQuestion.tsx
@src/pages/DrillPage.tsx
@src/utils/formatPrompt.ts
@src/components/ui/Button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate RightPanel to Tailwind and integrate structured prompt formatting</name>
  <files>src/components/RightPanel.tsx</files>
  <action>
Replace all CSS class references with Tailwind utilities. Import and use `formatPrompt` and `PromptDisplay` from `../utils/formatPrompt`.

**CRITICAL — Handsontable adjacency:** RightPanel is a direct sibling of the grid area in the flex layout. The panel's width (280px), min-width, and flex-shrink behavior MUST be preserved exactly. Use `w-[280px] min-w-[280px] shrink-0` to match the old `.right-panel` CSS.

Mapping of CSS classes to Tailwind (reference index.css lines 341-570):

**Panel container:**
- `.right-panel` → `w-[280px] min-w-[280px] border-l border-border bg-surface overflow-y-auto p-4 flex flex-col gap-3 shrink-0`

**Progress bar:**
- `.progress-bar-wrapper` → `flex flex-col gap-1`
- `.progress-label` → `text-[11px] text-muted font-semibold`
- `.progress-bar-track` → `h-1 bg-border rounded-sm overflow-hidden`
- `.progress-bar-fill` → `h-full bg-brand rounded-sm transition-[width] duration-300 ease-out` (keep inline style for dynamic width%)

**Timer:**
- `.challenge-timer` → `text-xs text-muted font-mono text-right -mt-2`

**Title:**
- `.challenge-title` → `text-[15px] font-bold text-brand-dark m-0 leading-tight`

**Prompt — REPLACE with PromptDisplay:**
- OLD: `<p className="challenge-prompt">{challenge.prompt}</p>`
- NEW: `<PromptDisplay sections={formatPrompt(challenge.prompt)} />`
- Import both from `../utils/formatPrompt`

**Hint:**
- `.btn.btn-hint` → Use `<button className="self-start px-2.5 py-2 text-[13px] font-semibold bg-blue-50 text-blue-800 rounded-btn border-none cursor-pointer transition-opacity duration-150 hover:opacity-85">Show Hint</button>`
- `.hint-box` → `text-[13px] bg-blue-50 border border-blue-200 rounded-btn px-2.5 py-2 text-blue-800`

**Feedback section:**
- `.feedback-section` → `flex flex-col gap-1.5`
- `.feedback-pending` → `text-[13px] text-muted m-0`
- `.feedback-correct` → `flex items-center gap-1.5 text-sm font-bold text-brand bg-brand-light rounded-btn py-2 px-3`
- `.feedback-checkmark` → `text-lg`
- `.feedback-incorrect` → `flex flex-col gap-1 text-[13px] text-red-600 bg-red-50 rounded-btn py-2 px-3`
- `.feedback-item` → `text-[13px]`
- `.error-code` → `font-mono bg-base px-1 py-px rounded text-xs text-red-600`

**Explanation accordion:**
- `.explanation-details` → `border border-border rounded-btn overflow-hidden`
- `.explanation-summary` → `text-[13px] font-semibold text-brand-dark py-2 px-3 cursor-pointer bg-base select-none hover:bg-border/40`
- `.explanation-body` → `py-2.5 px-3 flex flex-col gap-1.5`
- `.explanation-label` → `text-xs font-semibold text-muted m-0 uppercase tracking-wide`
- `.correct-formula` → `font-mono text-[13px] bg-brand-light text-brand-dark rounded-btn py-1 px-2 block break-all`
- `.explanation-text` → `text-xs text-muted leading-relaxed m-0 whitespace-pre-wrap`

**Navigation buttons:**
- `.nav-buttons` → `flex gap-2 flex-wrap mt-auto pt-2`
- Replace `.btn.btn-skip`, `.btn.btn-retry`, `.btn.btn-next` with Tailwind-styled buttons:
  - Skip: `px-3.5 py-2 text-[13px] font-semibold bg-base text-muted rounded-btn border-none cursor-pointer transition-opacity duration-150 hover:opacity-85`
  - Retry: `px-3.5 py-2 text-[13px] font-semibold bg-red-50 text-red-600 rounded-btn border border-red-200 cursor-pointer transition-opacity duration-150 hover:opacity-85`
  - Next: `flex-1 px-3.5 py-2 text-[13px] font-semibold bg-brand text-white rounded-btn border-none cursor-pointer transition-opacity duration-150 hover:opacity-85`

Remove the inline `style={{ color: '#888', fontSize: '14px' }}` from the loading state — use `text-muted text-sm` instead.
Remove the inline `style={{ color: '#1a6b3c' }}` from correct feedback items — use `text-brand` class.
  </action>
  <verify>
    cd /Users/jam/excel-prep && npx tsc --noEmit && echo "TypeScript OK"
  </verify>
  <done>RightPanel uses only Tailwind utilities. Challenge prompts render through PromptDisplay with structured scenario/data/task layout. Panel width and flex behavior preserved for Handsontable compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ChallengePage layout and DrillQuestionCard to Tailwind</name>
  <files>src/pages/ChallengePage.tsx, src/components/DrillQuestion.tsx, src/pages/DrillPage.tsx</files>
  <action>
**ChallengePage.tsx** — Replace CSS class references with Tailwind utilities.

CRITICAL: The three-column flex layout MUST preserve exact height propagation to Handsontable. The grid collapses if any parent div breaks the flex height chain.

Mapping (reference index.css lines 112-236):
- `.challenge-page` → `flex flex-row h-full overflow-hidden`
- `.challenge-sidebar` → `flex flex-col border-r border-border bg-base shrink-0 w-[200px] min-w-[200px]`
- `.challenge-grid-area` → `flex-1 overflow-hidden flex flex-col`
- `.tier-locked-message` → `flex flex-col items-center justify-center h-full gap-3 p-6 text-center text-muted`
- `.tier-locked-title` → `text-lg font-bold text-brand-dark m-0`
- `.tier-locked-body` → `text-sm m-0 max-w-[300px]`
- `.tier-locked-hint` → `text-xs text-muted m-0`

**DrillQuestionCard.tsx** — Convert ALL inline styles to Tailwind utilities. This component still uses dark-theme colors (rgba(255,255,255,...)) from v1.0 — convert to light theme matching the DrillPage restyle from Phase 7.

Typing mode:
- Scope hint div: `text-[13px] text-muted mb-2 italic`
- Form: `flex gap-2`
- Input: `flex-1 font-mono text-base px-3.5 py-2.5 rounded-lg border-2 border-border bg-surface text-text-primary outline-none focus:border-brand transition-colors duration-150`
- Submit button: `px-5 py-2.5 rounded-lg border-none bg-brand text-white text-sm font-semibold cursor-pointer disabled:cursor-not-allowed disabled:opacity-50`

Multiple choice mode:
- Container: `w-full flex flex-col gap-2.5`
- Option button: `block w-full px-4 py-3 rounded-lg border-2 border-border bg-surface text-text-primary font-mono text-sm text-left cursor-pointer disabled:cursor-not-allowed disabled:opacity-60 transition-all duration-150 hover:border-brand/50 hover:bg-brand-light/30`
- Remove the onMouseEnter/onMouseLeave inline style handlers — Tailwind hover: classes handle this

Remove ALL inline `style={{...}}` props. The component should have zero style props.

**DrillPage.tsx** — Apply structured prompt formatting to drill question prompts.

Currently line 189: `{currentQuestion.prompt}` renders as plain text.
Change to: Import `formatPrompt` and `PromptDisplay` from `../utils/formatPrompt`, then:
```tsx
<PromptDisplay sections={formatPrompt(currentQuestion.prompt)} />
```

The drill question `prompt` field uses the same format as challenge prompts (from the `drillPrompt` field in challenge data, which is a shorter version but follows similar patterns with data values separated by slashes). The formatPrompt utility will parse these appropriately — if no data block is detected, it falls back to rendering the whole string as scenario text, which is fine for shorter drill prompts.
  </action>
  <verify>
    cd /Users/jam/excel-prep && npx tsc --noEmit && npm run build 2>&1 | tail -5
  </verify>
  <done>ChallengePage layout uses Tailwind flex utilities preserving exact height propagation. DrillQuestionCard uses light-theme Tailwind utilities with zero inline styles. Drill prompts render through PromptDisplay. Build passes with no errors.</done>
</task>

<task type="auto">
  <name>Task 3: Remove old challenge CSS class definitions from index.css</name>
  <files>src/index.css</files>
  <action>
Remove ALL challenge-component CSS class definitions from index.css that were replaced by Tailwind utilities in Plans 01 and 02. These are lines ~112-681 in the current file.

**Sections to REMOVE (delete entirely):**
1. "Challenge Page Layout" section: `.challenge-page`, `.challenge-grid-area`
2. "Challenge Sidebar" section: `.challenge-sidebar`
3. "Tier Tabs" section: `.tier-tabs`, `.tier-tab`, `.tier-tab:hover`, `.tier-tab--active`, `.tier-tab--locked`, `.tier-tab--locked.tier-tab--active`, `.tier-tab-lock`, `.tier-tab-label`
4. "Tier locked state" section: `.tier-locked-message`, `.tier-locked-title`, `.tier-locked-body`, `.tier-locked-hint`
5. "Challenge List" section: `.challenge-list`, `.challenge-list-header`, `.challenge-list-items`, `.challenge-list-item`, `.challenge-list-item:hover`, `.challenge-list-item.active`, `.challenge-list-icon`, `.challenge-list-title`, `.challenge-list-title.text-locked`, `.challenge-list-item.locked`, `.challenge-list-item.locked:hover`, `.challenge-category-badge`, `.challenge-locked-msg`
6. "Right Panel" section: `.right-panel`, `.progress-bar-wrapper`, `.progress-label`, `.progress-bar-track`, `.progress-bar-fill`, `.challenge-timer`, `.challenge-title`, `.challenge-prompt`, `.hint-box`
7. "Feedback" section: `.feedback-section`, `.feedback-pending`, `.feedback-correct`, `.feedback-checkmark`, `.feedback-incorrect`, `.feedback-item`, `.error-code`
8. "Explanation accordion" section: `.explanation-details`, `.explanation-summary`, `.explanation-summary:hover`, `.explanation-body`, `.explanation-label`, `.correct-formula`, `.explanation-text`
9. "Navigation buttons" section: `.nav-buttons`, `.btn`, `.btn:hover`, `.btn-hint`, `.btn-skip`, `.btn-retry`, `.btn-next`
10. "Completion Screen" section: `.completion-screen`, `.completion-card`, `.completion-trophy`, `.completion-title`, `.completion-stats`, `.completion-stat`, `.completion-stat-value`, `.completion-stat-label`, `.completion-missed`, `.completion-missed-label`, `.completion-missed-list`, `.completion-missed-list li`, `.completion-buttons`, `.btn-review`, `.btn-home`

**Sections to KEEP (do NOT touch):**
- `@import "tailwindcss"` and `@theme` block (lines 1-17)
- Tailwind preflight repair for Handsontable (lines 19-25) — CRITICAL
- Reset margin/padding for full-height layout (lines 27-36)
- Feedback animations keyframes and classes (lines 38-64) — used by DrillFeedback
- Handsontable ht-theme-main CSS variable overrides (lines 66-102)
- Formula bar utility `.formula-bar` (line 104-108)
- The `completion-pop` keyframe animation (lines 603-607) — still referenced by CompletionScreen via arbitrary Tailwind animation value. KEEP this keyframe block.

After deletion, verify the file structure is clean: @import, @theme, preflight repair, reset, animations (correct-flash, incorrect-shake, completion-pop), HOT overrides, formula-bar. Nothing else.

**Verification after deletion:**
- Run `npm run build` to confirm no component references a deleted CSS class
- If build has warnings about unused classes, that's fine — we want NO errors about missing classes
  </action>
  <verify>
    cd /Users/jam/excel-prep && npm run build 2>&1 | tail -10 && echo "---" && wc -l src/index.css
  </verify>
  <done>index.css contains only: Tailwind import, @theme tokens, preflight repair, reset, animation keyframes (correct-flash, incorrect-shake, completion-pop), Handsontable overrides, and formula-bar. All challenge component CSS classes removed. Build passes cleanly. File is approximately 100-110 lines (down from ~681).</done>
</task>

</tasks>

<verification>
After all 3 tasks:
1. `npm run build` passes with zero errors — no component references deleted CSS classes
2. Challenge prompts in RightPanel render as structured sections (scenario / data block / task)
3. Drill prompts in DrillPage render through PromptDisplay
4. Handsontable grid renders at correct height with visible borders (verify by hard reload, not just HMR)
5. DrillQuestionCard has zero inline style props — all light-theme Tailwind
6. index.css is ~100-110 lines (down from ~681)
7. The three-column ChallengePage layout (sidebar | grid | right panel) maintains correct proportions
</verification>

<success_criteria>
- RightPanel shows challenge prompts with: light gray scenario text, monospace data block with bg-base, bold brand-dark task instruction
- DrillPage shows drill prompts with the same structured formatting
- ChallengePage flex layout preserves Handsontable grid height (no collapse)
- DrillQuestionCard uses light-theme colors matching Phase 7 DrillPage restyle
- index.css has no challenge-specific CSS class definitions (only tokens, resets, animations, HOT overrides)
- Full build (tsc + vite) passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/08-challenge-components-prompt-formatting/08-02-SUMMARY.md`
</output>
