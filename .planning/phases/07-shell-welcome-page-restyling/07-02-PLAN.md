---
phase: 07-shell-welcome-page-restyling
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/pages/DrillPage.tsx
  - src/components/DrillFeedback.tsx
  - src/components/DrillReview.tsx
  - src/index.css
autonomous: true
requirements: [VIS-02, UX-03]

must_haves:
  truths:
    - "DrillPage idle/active/review screens all use light theme (white/light surfaces) -- no #111827 dark background anywhere"
    - "Correct answer feedback shows green flash animation (scale-up + green overlay)"
    - "Incorrect answer feedback shows red shake animation (horizontal shake + red overlay)"
    - "DrillReview page uses light surfaces with green/red status colors on white background"
    - "All DrillPage text renders in Inter font via font-sans token"
    - "Timer urgent state (<=10s) still shows red color via inline style (dynamic value)"
  artifacts:
    - path: "src/pages/DrillPage.tsx"
      provides: "Light-themed drill page with all 3 phases (idle, active, review)"
      contains: "bg-surface"
    - path: "src/components/DrillFeedback.tsx"
      provides: "Feedback overlay with CSS animation classes"
      contains: "animate-"
    - path: "src/components/DrillReview.tsx"
      provides: "Light-themed review screen"
      contains: "bg-surface"
    - path: "src/index.css"
      provides: "CSS keyframe definitions for feedback animations"
      contains: "@keyframes correct-flash"
  key_links:
    - from: "src/components/DrillFeedback.tsx"
      to: "src/index.css"
      via: "CSS animation keyframes"
      pattern: "animate-correct-flash|animate-incorrect-shake"
    - from: "src/pages/DrillPage.tsx"
      to: "src/index.css"
      via: "Tailwind utility classes consuming @theme tokens"
      pattern: "bg-surface|bg-base|text-brand"
---

<objective>
Convert DrillPage from dark theme (#111827) to unified light theme using design tokens, and add CSS keyframe animations for correct/incorrect answer feedback.

Purpose: DrillPage is the single largest visual inconsistency -- it's the only dark-themed page. Removing the dark background (VIS-02) and adding feedback animations (UX-03) are core Phase 7 requirements.
Output: Light-themed DrillPage.tsx, animated DrillFeedback.tsx, light-themed DrillReview.tsx, and CSS keyframes in index.css.
</objective>

<execution_context>
@/Users/jam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-design-foundation/06-01-SUMMARY.md
@.planning/phases/07-shell-welcome-page-restyling/07-01-SUMMARY.md
@src/index.css
@src/components/ui/Button.tsx
@src/components/ui/Card.tsx
@src/pages/DrillPage.tsx
@src/components/DrillFeedback.tsx
@src/components/DrillReview.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add feedback animation keyframes to index.css</name>
  <files>src/index.css</files>
  <action>
Add CSS keyframe definitions and utility classes for correct/incorrect feedback animations. Place them AFTER the existing `@theme` block and preflight repair, but BEFORE the Handsontable overrides section.

Add this CSS block:

```css
/* -----------------------------------------------------------------------
   Feedback Animations (correct = green flash, incorrect = red shake)
   ----------------------------------------------------------------------- */

@keyframes correct-flash {
  0%   { opacity: 0; transform: scale(0.95); }
  40%  { opacity: 1; transform: scale(1.02); }
  100% { opacity: 1; transform: scale(1); }
}

@keyframes incorrect-shake {
  0%, 100% { transform: translateX(0); }
  15%      { transform: translateX(-6px); }
  30%      { transform: translateX(6px); }
  45%      { transform: translateX(-4px); }
  60%      { transform: translateX(4px); }
  75%      { transform: translateX(-2px); }
  90%      { transform: translateX(2px); }
}

.animate-correct-flash {
  animation: correct-flash 0.35s ease-out;
}

.animate-incorrect-shake {
  animation: incorrect-shake 0.4s ease-in-out;
}
```

Do NOT modify the @theme block, the preflight repair, or the Handsontable overrides. Only add the new keyframes block.
  </action>
  <verify>
    <automated>cd /Users/jam/excel-prep && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>index.css contains @keyframes correct-flash and incorrect-shake definitions with corresponding .animate-* utility classes.</done>
</task>

<task type="auto">
  <name>Task 2: Convert DrillPage, DrillFeedback, and DrillReview to light theme</name>
  <files>src/pages/DrillPage.tsx, src/components/DrillFeedback.tsx, src/components/DrillReview.tsx</files>
  <action>
Convert all three components from dark theme (inline styles with #111827 backgrounds, white/rgba text) to light theme using Tailwind utilities and @theme tokens. Import Button and Card where appropriate.

**DrillPage.tsx -- ALL THREE PHASES (idle, active, review):**

Add imports: `import { Button } from '../components/ui/Button'` and `import { Card } from '../components/ui/Card'`

IDLE phase (setup screen):
- Replace the dark outer div with: `<Card shadow className="max-w-[520px] mx-auto mt-10">`
- h1: `className="text-2xl font-bold text-brand-dark mb-1"`
- Subtitle p: `className="text-sm text-muted mb-8"`
- Section labels ("Difficulty", "Answer Mode"): `className="text-[11px] font-semibold text-muted uppercase tracking-wide mb-2.5 block"`
- Tier selector buttons: Convert from inline `style` to Tailwind. For selected state use `bg-brand-light border-2 border-brand text-brand-dark font-semibold`, for unselected use `bg-surface border-2 border-border text-text-primary/70 hover:border-brand/50`. Keep the `onClick` handlers.
- Mode toggle container: `className="flex bg-base rounded-lg p-1 w-fit"`
- Mode toggle buttons: Selected = `bg-brand text-white`, unselected = `bg-transparent text-muted`. Use `className` with clsx.
- Timer info box: `className="bg-base rounded-lg px-4 py-3 mb-7 text-sm text-muted"`
- Start Drill button: Replace with `<Button className="w-full py-3.5 text-base font-bold rounded-[10px]" onClick={...}>Start Drill</Button>`

REVIEW phase:
- Replace dark outer div with: `<Card shadow className="max-w-[680px] mx-auto mt-8 overflow-y-auto max-h-[calc(100vh-100px)]">`
- h1 "Session Complete": `className="text-xl font-bold text-brand-dark mb-6 text-center"`

ACTIVE + FEEDBACK phase:
- Replace dark outer div with: `<Card shadow className="max-w-[640px] mx-auto mt-8 relative">`
- Header spans (Question N/total, Score): `className="text-[13px] text-muted"` with inner spans `className="text-text-primary font-semibold"` and score value `className="text-brand font-semibold"`
- Timer: Large number uses `className="inline-block text-[40px] font-bold tabular-nums leading-none transition-colors duration-300"` with INLINE style ONLY for the dynamic urgent color: `style={{ color: isUrgent ? '#ef4444' : 'var(--color-text-primary)' }}` (this is a legitimate dynamic value)
- "seconds" label: `className="text-[11px] text-muted mt-0.5 tracking-wide"`
- Category badge: `className="inline-block text-[11px] font-semibold text-muted uppercase tracking-wide bg-base px-2 py-0.5 rounded"`
- Question prompt: `className="text-base leading-relaxed text-text-primary mb-6 min-h-[60px]"`

Remove ALL other `style={{}}` props. The ONLY inline style that should remain is the timer color for the dynamic urgent state.

**DrillFeedback.tsx:**
Convert from inline styles to Tailwind utilities. Remove the embedded `<style>` tag (keyframes now live in index.css).

- Outer overlay div: Use conditional classes based on `isCorrect`:
  ```tsx
  className={clsx(
    'absolute inset-0 flex flex-col items-center justify-center rounded-card z-10 p-6 text-center',
    isCorrect ? 'bg-green-800/95 animate-correct-flash' : 'bg-red-800/95 animate-incorrect-shake'
  )}
  ```
- Checkmark/X icon: `className="text-5xl mb-2 leading-none"`
- "Correct!"/"Incorrect" text: `className="text-2xl font-bold text-white mb-4"`
- "Correct answer" label: `className="text-[13px] text-white/75 uppercase tracking-wide mb-1.5"`
- Formula display: `className="font-mono text-[15px] text-white bg-black/30 px-4 py-2 rounded-md break-all max-w-[480px]"`

Import clsx: `import { clsx } from 'clsx'`

Delete the entire `<style>{\`@keyframes fadeIn...\`}</style>` block.

**DrillReview.tsx:**
Convert from dark theme to light theme using Tailwind utilities. Import Button.

Add imports: `import { Button } from './ui/Button'` (path is ../components/ui/Button relative but DrillReview is in components/ already, so: `import { Button } from './ui/Button'`)

- Outer div: `className="w-full max-w-[640px] mx-auto"`
- Score summary box: `className="text-center mb-8 p-6 bg-base rounded-xl border border-border"`
- Score number: `className="text-5xl font-bold leading-none mb-2"` with `style={{ color: scoreColor }}` (dynamic -- keep inline)
- Percentage text: `className="text-xl text-muted"`
- Per-question list: `className="flex flex-col gap-2 mb-7"`
- Question row borders: Convert from inline rgba to Tailwind -- correct: `border border-green-600/30`, incorrect: `border border-red-600/30`
- Question row button backgrounds: correct = `bg-green-600/10`, incorrect = `bg-red-600/10`. Text colors: `text-text-primary` (not white anymore since light theme)
- Expanded detail panel: `className="p-3.5 px-4 bg-base border-t border-border flex flex-col gap-2"` (was dark rgba)
- Detail text spans: `text-muted` for labels, `text-text-primary` for values, `text-brand` for correct formula (was #4ade80 on dark)
- Explanation text: `className="text-[13px] text-muted mt-1 leading-relaxed"`
- Action buttons: Replace inline `<button>` with `<Button onClick={endSession}>Try Again</Button>` and replace the `<Link>` with `<Button variant="ghost" onClick={() => navigate('/challenge')}>Back to Challenges</Button>` (import useNavigate from react-router-dom, remove the Link import if no longer needed). Or keep `<Link>` but style it with Button-like classes: `className="px-6 py-2.5 rounded-btn border border-border text-muted text-sm font-medium no-underline inline-flex items-center hover:bg-base"`.

Choose the simpler approach: keep the Link element but apply Tailwind classes directly rather than importing Button (since Button renders a `<button>`, not an `<a>`).

Remove ALL dark-theme `style={{}}` props. Only `style={{ color: scoreColor }}` on the score number should remain (dynamic value).
  </action>
  <verify>
    <automated>cd /Users/jam/excel-prep && npm run build 2>&1 | tail -10</automated>
    <manual>Visit localhost:5173/drill. All 3 phases (idle setup, active question, review) should have white/light backgrounds. No #111827 dark backgrounds. Start a drill, answer correctly -- should see green flash animation. Answer incorrectly -- should see red shake animation.</manual>
  </verify>
  <done>DrillPage has zero #111827 backgrounds. All 3 phases use light theme (bg-surface, bg-base). DrillFeedback uses .animate-correct-flash and .animate-incorrect-shake CSS classes. DrillReview uses light surfaces. Build passes with zero errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. DrillPage idle screen: white card on light background, green accent buttons
3. DrillPage active screen: white card, dark text, timer turns red when urgent
4. DrillPage review screen: white card with green/red status indicators
5. Correct answer: green overlay with scale-up flash animation
6. Incorrect answer: red overlay with horizontal shake animation
7. No occurrence of `#111827` in DrillPage.tsx, DrillFeedback.tsx, or DrillReview.tsx
8. Inter font renders on all drill text
</verification>

<success_criteria>
- Zero occurrences of #111827 in DrillPage.tsx
- DrillFeedback.tsx uses animate-correct-flash / animate-incorrect-shake CSS classes (not inline keyframes)
- DrillReview.tsx uses light surfaces (bg-surface, bg-base) instead of dark rgba backgrounds
- Green flash visible on correct answer; red shake visible on incorrect answer
- Timer urgent color (#ef4444) still works as inline style for dynamic state
- Production build passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-shell-welcome-page-restyling/07-02-SUMMARY.md`
</output>
