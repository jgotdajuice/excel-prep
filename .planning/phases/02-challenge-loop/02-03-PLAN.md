---
phase: 02-challenge-loop
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: []
autonomous: false
requirements:
  - GRID-03
  - GRID-04
  - LEARN-01
  - LEARN-03
  - LEARN-04

must_haves:
  truths:
    - "End-to-end challenge flow works: load → enter formula → grade → feedback → navigate"
    - "All five Phase 2 success criteria verified by human tester"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete challenge loop. Confirm all five Phase 2 success criteria are met by playing through the seed challenges.

Purpose: Catch visual, UX, or behavioral issues that automated checks cannot detect.
Output: Verification result — approved or issues list for gap closure.
</objective>

<execution_context>
@/Users/jam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-challenge-loop/02-CONTEXT.md
@.planning/phases/02-challenge-loop/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify end-to-end challenge flow</name>
  <files>none (verification only)</files>
  <action>
Run `npm run dev` and open http://localhost:5173/challenge. Play through all seed challenges to verify the five Phase 2 success criteria.

What was built: Complete challenge loop — finance-scenario challenges with pre-populated grid, seed cell locking, answer cell highlighting, Enter-only grading, right panel feedback (correct/incorrect/error), expandable explanation with formula and interview tips, challenge navigation (next/skip/retry/back), challenge list with status icons, progress bar, informational timer, and completion screen.

Verification steps:

1. Run `npm run dev` and open http://localhost:5173 in your browser
2. Click "Start Challenge" (or navigate to http://localhost:5173/challenge)

**Test Challenge 1 (VLOOKUP):**
3. Verify: finance-scenario prompt is visible in right panel (not a generic example)
4. Verify: grid shows labeled seed data (Employee ID, Name, Salary) — seed cells are NOT editable (try clicking one)
5. Verify: answer cell has a blue border distinguishing it from seed cells
6. Click the answer cell and type a WRONG formula: `=SUM(A1:A3)` then press Enter
7. Verify: right panel shows "Expected: $X — Got: $Y" with both values
8. Verify: answer cell border turns red
9. Verify: grid is locked (cannot edit any cell)
10. Click "Try Again" — verify answer cell clears, grid unlocks, border returns to blue
11. Type the CORRECT formula: `=VLOOKUP(103,A2:C5,3,0)` then press Enter
12. Verify: green checkmark + "Correct!" in right panel, green answer cell border
13. Click "See explanation" — verify it shows the correct formula and an interview tip
14. Verify: clicking away from a cell does NOT trigger grading (only Enter does)

**Test error handling:**
15. Click "Try Again", type `=VLOKUP(103,A2:C5,3,0)` (misspelled), press Enter
16. Verify: Excel error code (#NAME?) appears in the cell — NOT a plain English translation

**Test navigation:**
17. Click "Next Challenge" — verify Challenge 2 loads with new seed data
18. Click "Skip" — verify it advances and "skipped" status shows in challenge list
19. Click a challenge in the list — verify it jumps to that challenge
20. Verify progress bar shows correct count (e.g., "1/3")

**Test completion:**
21. Complete or skip all challenges
22. Verify: completion screen shows correct count, time spent, and missed questions
23. Verify: "Back to Home" button works

**Test hint:**
24. Go back to an unattempted challenge
25. Click "Show Hint" — verify function name is revealed (e.g., "Try using: VLOOKUP")
  </action>
  <verify>Type "approved" if all checks pass, or describe specific issues that need fixing.</verify>
  <done>Human tester confirms all 5 Phase 2 success criteria are met and the challenge flow works end-to-end.</done>
</task>

</tasks>

<verification>
All five Phase 2 success criteria from ROADMAP.md:
1. User is presented with a finance-scenario prompt and a pre-populated grid with relevant seed data
2. After submitting a formula, user sees whether their computed output matches the expected value
3. User sees distinct feedback for "wrong value" versus "formula syntax error"
4. After every submission, user sees an explanation of how the formula works
5. User can move to the next challenge after seeing their result
</verification>

<success_criteria>
Human tester confirms all 5 success criteria are met and the challenge flow feels natural.
</success_criteria>

<output>
After completion, create `.planning/phases/02-challenge-loop/02-03-SUMMARY.md`
</output>
