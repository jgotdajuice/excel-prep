---
phase: 09-deployment-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03]

must_haves:
  truths:
    - "Production build (tsc -b && vite build) completes with zero errors"
    - "App is live on a Vercel .vercel.app URL"
    - "All routes (/, /challenge, /drill, /shortcuts, /progress) load correctly on direct URL access"
    - "Challenge flow works end-to-end on production: load challenge, enter formula, see grade + explanation"
    - "Progress persists across page refreshes on production (localStorage survives)"
  artifacts:
    - path: "dist/index.html"
      provides: "Production build output"
    - path: "vercel.json"
      provides: "SPA rewrite rule for client-side routing"
      contains: "rewrites"
  key_links:
    - from: "vercel.json"
      to: "dist/index.html"
      via: "SPA rewrite catches all routes"
      pattern: "source.*destination.*index.html"
    - from: "dist/assets/index-*.js"
      to: "react-router-dom BrowserRouter"
      via: "client-side routing handles /challenge, /drill, etc."
      pattern: "BrowserRouter"
---

<objective>
Deploy the polished ExcelPrep app to Vercel and verify all routes and features work in production.

Purpose: Ship v1.1 as a live, accessible web app completing the Polish and Deploy milestone.
Output: Live Vercel URL with all routes working, production build validated.
</objective>

<execution_context>
@/Users/jam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build validation and Vercel deploy</name>
  <files></files>
  <action>
1. Run `npm run build` (which runs `tsc -b && vite build`) and confirm zero errors. Capture the output.

2. Deploy to Vercel using CLI. Run: `npx vercel --yes` from the project root (`/Users/jam/excel-prep`).
   - The `--yes` flag accepts defaults for project setup prompts.
   - If Vercel CLI prompts for login/auth, STOP and create a checkpoint for the user to authenticate. The user can run `npx vercel login` interactively.
   - vercel.json already exists with SPA rewrite rule — no changes needed.
   - No files are modified; this is a CLI-only deployment task.

3. After deploy completes, capture the production URL from the CLI output (will be a .vercel.app URL).

4. Run `npx vercel --prod --yes` to promote to production deployment (the first deploy creates a preview URL; --prod makes it the production URL).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 | grep -q "built in" && echo "BUILD OK" || echo "BUILD FAILED"</automated>
    <manual>Vercel CLI output shows a live URL</manual>
  </verify>
  <done>npm run build passes with zero errors. Vercel deploy completes and outputs a live production URL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Production route and feature verification</name>
  <files></files>
  <action>
Human verifies the deployed app. No automated action — this is a manual verification checkpoint.
  </action>
  <verify>User confirms all routes and features work on production.</verify>
  <done>All 6 routes load on direct URL access, challenge flow works end-to-end, progress persists across refresh.</done>
  <what-built>ExcelPrep deployed to Vercel with SPA routing</what-built>
  <how-to-verify>
    1. Open the Vercel production URL in a browser
    2. Verify the landing page (/) loads — should show WelcomePage with "How it works" section
    3. Navigate to /challenge via the sidebar — challenge list and grid should render
    4. Navigate to /drill via the sidebar — drill setup page should render
    5. Navigate to /shortcuts via the sidebar — shortcuts page should render
    6. Navigate to /progress via the sidebar — progress page should render
    7. Test direct URL access: paste {vercel-url}/challenge directly in the address bar — should load correctly (not 404)
    8. Test direct URL access: paste {vercel-url}/drill directly in the address bar — should load correctly (not 404)
    9. Test challenge flow: start a challenge, enter a formula in the answer cell, press Enter, confirm grading + explanation appear
    10. Refresh the page after completing at least one challenge — confirm progress is retained (localStorage persistence)
  </how-to-verify>
  <resume-signal>Type "approved" if all routes and features work, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0 with "built in" message (DEPLOY-03)
- Vercel production URL is accessible (DEPLOY-01)
- All 6 routes load on direct access: /, /challenge, /drill, /practice, /shortcuts, /progress (DEPLOY-02)
- Challenge end-to-end flow works on production
- localStorage persistence works on production
</verification>

<success_criteria>
- Production build passes with zero TypeScript and Vite errors
- App is live on a Vercel .vercel.app production URL
- All client-side routes resolve correctly on direct URL access (no 404s)
- Challenge flow works end-to-end on production
- Progress persists across page refreshes
</success_criteria>

<output>
After completion, create `.planning/phases/09-deployment-and-verification/09-01-SUMMARY.md`
</output>
